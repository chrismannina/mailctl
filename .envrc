#!/bin/bash
# Auto-create and activate Python virtual environment with uv

if [[ ! -d ".venv" ]]; then
    echo "Creating virtual environment with uv..."
    uv venv
fi

source .venv/bin/activate

echo "Python virtual environment activated"
echo "Python path: $(which python)"